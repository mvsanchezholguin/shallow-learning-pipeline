# shallow-learning-pipeline
End-to-end supervised machine learning pipeline focused on shallow learning methods. The repository implements feature selection techniques, baseline model benchmarking, hyperparameter optimization, ensemble learning, diversity analysis, and overfitting assessment using independent train, tuning, and external validation datasets.
